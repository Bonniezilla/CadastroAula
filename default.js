const cadastroHtml = 'cadastro.html';

const buttonEl = document.getElementById['buttonEl'];

buttonEl.onclick = function() {
    redirecione();
}

function redirecione() {
    window.location.replace(cadastroHtml);
}